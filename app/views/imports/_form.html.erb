<%= form_with(model: import, local: true) do |form| %>
  <% if import.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(import.errors.count, "error") %> prohibited this import from being saved:</h2>

      <ul>
      <% import.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    
  <div class="field">
    <%= form.label :file %>
    <%= form.file_field :file %>
  </div>

  <div class="field">
    <%= form.label :create_category %>
    <%= form.check_box :create_category, id: :import_create_category %>
  </div>

  <div class="field">
    <%= form.label :override_existing %>
    <%= form.check_box :override_existing, id: :import_override_existing %>
  </div>

  <div class="field">
    <%= form.label :notify_on_finish %>
    <%= form.check_box :notify_on_finish, id: :import_notify_on_finish %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
